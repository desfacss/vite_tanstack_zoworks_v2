import React from 'react';
import MappingContainer from '../components/maps';

/**
 * TrackingMigrationPage
 * Full Field Service Management (FSM) Tracking & Geofencing suite.
 * Features: Drawing tools, Agent tracking, Customer boundaries, History.
 */
const TrackingMigrationPage: React.FC = () => {
    return <MappingContainer />;
};

export default TrackingMigrationPage;
